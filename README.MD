A simple Node.js + Express + MySQL web application that lets users register, log in, and record fitness activities such as running, walking, and cycling.

Features
    User registration with validation
    Secure password hashing using bcrypt
    Login system with session-based authentication
    Add new fitness activities
    View all activities in a styled list
    Success pages with navigation options
    Clean UI with a custom CSS theme
